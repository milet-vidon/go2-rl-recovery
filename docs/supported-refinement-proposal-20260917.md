# Supported refinement after preserved recovery

Bounded new inference experiment, not new training or accepted capability. Both trained3746 and earlier3600 fail the same backtrial13(state131) when replacing stand3547 directly. They remain rejected as drop-in recovery-standing replacements. No more checkpoint scanning or unchanged training extension.

Hypothesis: use the frozen roll1999/originalstand3547 through the actual recovery, then route to trainednatural3746 only AFTER150consecutive completed50Hzintervals meet the unchanged full strict stance criterion. This uses three actors, explicitly. It is not a single improved recovery policy or a reproduction of animal-motion imitation. Commands remain zero; locomotion compatibility is independently unresolved.

The gate observes actual current state/support/geometry only, never pose class, bankID, old outcome or a selected successful example. Require the old stand branch active. Gate calculated AFTER env.step affects only the NEXT issued action. No new PD, interpolation, retry, reset, teleport, action/observation-history clearing or physical change at either switch. Original1s initial nominal-PD protocol remains disclosed and unchanged. Third actor construction must preserve original CPU/CUDA random generators. Preserve the original native48/input12/output/action-history and nominal+.25softclamp semantics.

Run one OFF back20 instrumented control first, require exact original3547common outcomes and every available historical trace/CSV value. Store full all-trial every-step evidence in streamingJSONL; no hypothetical replay. Then ON back20. Only if all20finalholds remain valid AND all20have150valid completed intervals controlled by the refinement actor at the end, proceed to upright20/side20. Gate reaching3s cannot itself count as post-refinement success. Original recovery-time score and post-refinement stability/width/asymmetry are separate. Check current-force continuity, full action/target history, new-boundary jumps, final widths, torque and observed motion. Reject any loss of strict final holds. Do not silently relax previous retention thresholds or discard failed trials.

The ON controller naturally postpones final stylistic refinement until after a3s verified recovery hold; disclose this latency. Repeated development states do not prove new-start generalization. If successful, record and actually inspect front/oblique real replays of the complete11s zero-command scenario. These would be recovery/refinement demonstrations, NOT the requested full walk/run/fall/recover workflow, which remains pending separate compatibility and locomotion training.
